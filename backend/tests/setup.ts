import { jest } from '@jest/globals';

// Set default timeout if needed
jest.setTimeout(10000);

// Silence console logs during tests if desired, but for now we keep them to see what happens
// console.log = jest.fn();
